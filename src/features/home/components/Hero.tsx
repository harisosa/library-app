import { Carousel } from "@/features/home/components/Carousel";
import { Section } from "@/shared/components/layout";



export const Hero = () => {
    return (
        <Section>
            <Carousel />
        </Section>
    );
};
